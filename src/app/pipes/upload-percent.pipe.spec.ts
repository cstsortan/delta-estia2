import { UploadPercentPipe } from './upload-percent.pipe';

describe('UploadPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new UploadPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
